global:
  scrape_interval: 15s

scrape_configs:
  - job_name: orderbook-api
    metrics_path: /metrics
    static_configs:
      # Replace with the address of the API from the Prometheus network.
       - targets: ["api:8080"]
